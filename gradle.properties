org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8

org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Published Maven coordinates, read by the vanniktech publish plugin. The release workflow
# checks the tag against VERSION_NAME.
GROUP=krd.pass
POM_ARTIFACT_ID=krdpass-auth
VERSION_NAME=1.6.0
